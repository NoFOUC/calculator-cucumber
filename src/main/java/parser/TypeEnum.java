package parser;

public enum TypeEnum {

    // make class enum for the type of the number (IntegerValue, RationalValue, RealValue)

    IntegerValue,
    RationalValue,
    RealValue,
    ComplexValue,
    ComplexRealValue
}
